  <!-- Internships Section -->
        <!-- <section id="internships" class="mb-5">
          <div class="card shadow-sm">
            <div class="card-header bg-success text-white">Available Internships</div>
            <div class="card-body">
              <% if (internships.length > 0) { %>
  <% internships.forEach(internship => { %>
    <div class="list-group-item d-flex justify-content-between align-items-center">
      <div> 
        <h6><%= internship.title %> - <%= internship.company_name %></h6>
        <small>Duration: <%= internship.duration %> | Stipend: ₹<%= internship.stipend %></small>
      </div>
      <a href="/applications/apply/<%= internship.internship_id %>" class="btn btn-sm btn-success">Apply</a>
    </div>
  <% }) %>
<% } else { %>
  <p class="text-muted">No internships available at the moment.</p>
<% } %>
            </div>
          </div>
        </section> -->

        <!-- Applications Section -->
        <!-- <section id="applications">
          <div class="card shadow-sm">
            <div class="card-header bg-warning">My Applications</div>
            <div class="card-body">
              <% if (applications.length > 0) { %>
                <table class="table table-striped">
                  <thead>
                    <tr>
                      <th>Internship</th>
                      <th>Company</th>
                      <th>Status</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% applications.forEach(app => { %>
                      <tr>
                        <td><%= app.title %></td>
                        <td><%= app.company_name %></td>
                        <td>
                          <% if (app.status === 'Pending') { %>
                            <span class="badge bg-secondary">Pending</span>
                          <% } else if (app.status === 'Accepted') { %>
                            <span class="badge bg-success">Accepted</span>
                          <% } else { %>
                            <span class="badge bg-danger">Rejected</span>
                          <% } %>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              <% } else { %>
                <p class="text-muted">You have not applied to any internships yet.</p>
              <% } %>
            </div>
          </div>
        </section> -->